NEXT_PUBLIC_API_URL=http://localhost:4000
# Login/signout now live on auth-service (auth.wayneclub.com), not the PassBar backend.
NEXT_PUBLIC_AUTH_SERVICE_URL=http://localhost:4010

GOOGLE_GENAI_API_KEY=
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash
NEXT_PUBLIC_BASE_PATH=

# ── Web Push (notifications) ────────────────────────────────────────────────
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:support@passbar.app
# Shared secret used to authorize the Vercel Cron call to /api/cron/notifications
CRON_SECRET=
